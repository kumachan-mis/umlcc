#!/ bin / bash

staged_files = $(git diff-- name - only-- cached-- diff - filter = d) echo ${staged_files} |
               grep - E "^.*\.[ch]$" | xargs clang - format - i git add $ {
    staged_files
}
